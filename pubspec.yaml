name: quluko_ffmpeg_wrapper
description: A wrapper around FfmpegKit for Quluko app. (still have to use the open source version of the ffmpeg kit)
version: 2.0.2
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  ffmpeg_kit_flutter_new: 3.2.0   # This will be changed to use the open source version.
